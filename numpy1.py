''' Numpy is a python library used for working with arrays
Use of m=numpy --> in python we have lists that serve the purpose of arrays but they are slow to process.
'''

import numpy as np

arr = np.array([1,2,3,4,5])
print(arr)

print(np.__version__)